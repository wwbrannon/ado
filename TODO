Roadmap to v1.0:

Immediate TODO:
    o) finish the rest of the grammar
    o) incorporate most of the currently unused keyword tokens
       into the right places in the grammar
    o) finish the actions for commands

Semantics:
    o) write up stubs for all R functions implementing Stata commands

Write a test suite and test

Write documentation - man entries, vignettes, examples

* Edit the exports in 'NAMESPACE', and add necessary imports.
* Run R CMD build to build the package tarball.
* Run R CMD check to check the package tarball.

